name: send-message-on-message
vars:
  - variable1: "variables1"
  - variable2: "variables2"
activities:
  - send-message:
      id: "sendmessageid1"
      description: "activity to send a message when a /command is sent to the bot"
      on:
        message:
          content: "/message"
      to:
        streamId: "123"
      content: |
        <messageML>Hello!</messageML>
