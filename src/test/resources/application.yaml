#bdk:
#  datafeed:
#    enabled: false
logging:
  level:
    com.symphony: DEBUG
    org.camunda: DEBUG
    org.camunda.bpmn.engine.persistence: INFO

camunda:
  bpm:
    job-execution:
      wait-time-in-millis: 1000
