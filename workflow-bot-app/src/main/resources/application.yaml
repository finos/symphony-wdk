# Bot configuration
workflows:
  folder: ./workflows

# BDK configuration
bdk:
  host: devx3.symphony.com
  datafeed:
    version: v1
  bot:
    username: SoufianeBot2
    privateKey:
      path: /Users/soufiane/.symphony/rsa-devx3/privatekey.pem

#  pod:
#    host: devx3.symphony.com
#  agent:
#    host: localhost.symphony.com
#    port: 9443

# Camunda configuration
camunda:
  bpm:
    # Job execution is configured to pick up expired/scheduled activities quickly
    job-execution:
      wait-time-in-millis: 1000
      max-wait: 1000
    # Remove WARN logs at startup (we are not using this feature but our own watcher)
    deployment-resource-pattern:
    generic-properties:
      properties:
        # Avoid requests to the outside world
        telemetryReporterActivate: false

# Spring boot configuration
spring:
  main:
    banner-mode: off
logging:
  level:
    com.symphony: DEBUG
    # troubleshoot Camunda
    org.camunda: DEBUG
    # disable audit trail
    #audit-trail: WARN

# Actuator endpoint is enabled by default on port 8080
management:
  health:
    camunda:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"

