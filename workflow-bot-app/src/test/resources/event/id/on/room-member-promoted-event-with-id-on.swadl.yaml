id: room-member-promoted-event-with-id-in-on
activities:
  - execute-script:
      id: scriptActivity
      on:
        room-member-promoted-to-owner:
          id: roomMemberPromotedWithIdIn
      script: |

  - execute-script:
      id: scriptAssertion
      script: |
        assert roomMemberPromotedWithIdIn.source != null
