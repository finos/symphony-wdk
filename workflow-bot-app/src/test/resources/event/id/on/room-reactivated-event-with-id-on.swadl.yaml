id: room-reactivated-event-with-id-in-on
activities:
  - execute-script:
      id: scriptActivity
      on:
        room-reactivated:
          id: roomReactivatedWithIdIn
      script: |

  - execute-script:
      id: scriptAssertion
      script: |
        assert roomReactivatedWithIdIn.source != null
